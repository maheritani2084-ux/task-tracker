| Flow | Code sections likely affected | Verification step |
| --- | --- | --- |
| Add due date in task modal | Modal markup in [frontend/index.html](frontend/index.html), form state initialization/reset, saveTask payload assembly, form validation/error handling | Open the modal, create and edit a task with a valid due date, confirm the request payload includes it and validation errors still surface correctly |
| Front-end validation for invalid date format | Date input handling in the modal, client-side validation before submit, inline error messaging in [frontend/index.html](frontend/index.html) | Enter an invalid date format such as 2025/13/01 or not-a-date, confirm the form blocks submission and shows a clear message before the request reaches the API |
| Show due date and overdue indicator on cards | Card rendering in createCard, task data mapping, inline styling/CSS in [frontend/index.html](frontend/index.html) | Create tasks with past, today, and future due dates, then confirm cards display the date and an overdue pill only when appropriate |
| Add overdue filter or visual indicator in board view | Header area, board rendering logic, filter controls, and task list filtering in [frontend/index.html](frontend/index.html) | Toggle the overdue filter/indicator and confirm only overdue tasks appear or are highlighted while the existing board columns and counts remain intact |
| Preserve existing Kanban behaviors during edits and moves | Drag-and-drop handlers, moveTask rollback path, loading/empty/ready/error state rendering, fetchTasks in [frontend/index.html](frontend/index.html) | Drag a card to another column, verify it rolls back correctly on a rejected PATCH, and confirm loading, empty, ready, and error states still behave as before |
| Reuse existing API behavior without new backend endpoints | Existing GET/POST/PATCH calls and response handling in [frontend/index.html](frontend/index.html) | Confirm the UI uses the existing task endpoints for create, edit, and status updates and that due-date changes appear without introducing any new routes |
